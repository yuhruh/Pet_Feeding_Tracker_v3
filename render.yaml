# Exported from Render on 2025-07-23T09:08:25Z
services:
- type: web
  name: Pet_Feeding_Tracker_v3
  runtime: ruby
  repo: https://github.com/yuhruh/Pet_Feeding_Tracker_v3
  plan: free
  region: oregon
  # buildCommand: bundle install && bundle exec rake assets:precompile
  startCommand: bundle exec rails server -b 0.0.0.0 -p 3000
  # migrateCommand: bundle exec rails db:migrate
  envVars:
  - key: DATABASE_URL
    fromDatabase:
      name: Pet_Feeding_Tracker_v3-db
      property: connectionString
  - key: NODE_VERSION
    sync: false
  - key: RAILS_MASTER_KEY
    sync: false
  - key: WEB_CONCURRENCY
    value: 2
    sync: false
  buildCommand: bin/render-build.sh; bundle exec rails db:migrate
  # startCommand: bundle exec rails server
  autoDeployTrigger: commit
  previews:
    generation: automatic
version: "1"

databases:
  - name: Pet_Feeding_Tracker_v3-db
    plan: free
