<div class="container text-center mb-2 mt-2">
  <div class="px-6 py-4 border border-dark-subtle rounded-3">
    <h1><%= t('user_mailer.welcome.title', username: @user.username) %></h1>

    <p>
      <%= t('user_mailer.welcome.description') %>
    </p>

    <p>
      <%= t('user_mailer.welcome.login_prompt') %> <%= link_to t('user_mailer.welcome.login'), new_session_url %>
    </p>
  </div>
</div>